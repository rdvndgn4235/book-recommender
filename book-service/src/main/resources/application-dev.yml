server:
  port: 8082
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bookdb
    username: postgres
    password: postgres

  cache:
    type: redis

  data:
    redis:
      host: localhost
      port: 6379
    mongodb:
      uri: mongodb://mongoadmin:secret@localhost:27017/user-logs-db?authSource=admin
      database: book-logs-db